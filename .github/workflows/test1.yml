name: workflow test
on:
  workflow_dispatch:
  push:
jobs:
  init:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.init.outputs.matrix }}
    steps:
      - id: init
        run: |
          JSON='[{ "url": "https://blabla.com", "repositories": ["xxx/test1", "xxx/test2"] }, { "url": "https://pouet.com", "repositories": ["xxx/test3", "xxx/test4"] }]'
          echo ::set-output name=matrix::$JSON
  
  job1:
    runs-on: ubuntu-latest
    needs: init
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        url: ${{ fromJson(needs.init.outputs.matrix) }}
    steps:
      - run: |
          echo {{ matrix.url }}
